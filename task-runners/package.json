{
	"name": "task-runner-refresh-roster",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env sync-memberships && wrangler deploy --env process-roster && wrangler deploy --env process-certificates",
		"dev": "wrangler dev --test-scheduled",
		"start": "wrangler dev --test-scheduled",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.33.2"
	}
}
